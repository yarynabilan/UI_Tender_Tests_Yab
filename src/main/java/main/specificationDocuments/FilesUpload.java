package main.specificationDocuments;

import main.PublicTenderPage;
import org.openqa.selenium.WebDriver;

public class FilesUpload extends PublicTenderPage {

        public FilesUpload(WebDriver driver) { super(driver);
    }
}
